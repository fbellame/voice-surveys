apiVersion: v1
kind: Service
metadata:
  name: livekit-agent
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: livekit-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
